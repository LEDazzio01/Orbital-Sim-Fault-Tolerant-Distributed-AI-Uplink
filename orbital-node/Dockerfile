# Orbital Node - Semantic Kernel Worker (The Satellite)
FROM python:3.11-slim

WORKDIR /app

COPY src/ ./src/

# Install dependencies (add requirements.txt when ready)
# RUN pip install --no-cache-dir -r requirements.txt

CMD ["python", "-m", "src.main"]
